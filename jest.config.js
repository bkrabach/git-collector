/**
 * Jest configuration for git-collector project
 */
module.exports = {
  testEnvironment: 'node',
  testMatch: ['**/test/**/*.test.js'],
  // Automatically clear mock calls and instances between every test
  clearMocks: true,
};